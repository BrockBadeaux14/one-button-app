# One Button App

A Flutter app with a single centered button labeled “Press me.” The button is enabled, but pressing it does nothing.

## Run

```sh
flutter pub get
flutter run
```

Choose an available device when prompted, or use `flutter run -d chrome` to run in Chrome.
